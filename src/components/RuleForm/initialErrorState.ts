
export const initialErrorState={
    triggerDevice: false,
    triggerPeripheral: false,
    triggerEvent:false,
    targetDevice: false,
    targetPeripheral: false,
    targetAction: false,
    extraSettings: false,
    condition:false
}